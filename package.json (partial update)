
{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:client": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "vercel-build": "pnpm run build"
  }
}
